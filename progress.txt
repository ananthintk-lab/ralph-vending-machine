2026-07-01: Converted skeleton project from Spring Boot (Initializr default) to plain Java per CLAUDE.md architecture — removed spring-boot-starter-parent/JPA deps, RalphVendingMachineApplication.java, its Spring test, and application.properties, since they contradicted the "plain Java, no frameworks" requirement and the JPA test was failing at runtime with no datasource. Note: JDK 25 (required by CLAUDE.md) is not installed on this machine; only JDK 21 is available, so pom.xml targets release 21 for now.
2026-07-01: [Story: Product enum] Implemented dev.venga.ralph_vending_machine.model.Product enum with displayName and price (BigDecimal) fields for Cola, Diet Cola, Water, Orange Juice, Chips, Candy Bar, Gum. No unit test added (trivial enum with only getters, per CLAUDE.md testing guidance to skip simple enums without logic). Compile and test both pass.
